#include "map.h"
#include "snake.h"
#include "apple.h"

struct apple *apple_new(void) {
  struct apple *a;
  return a;
}

void apple_set_new_position(struct apple *a, struct map *m,
                            struct snake_body **sb) {
}

void apple_draw(struct apple *a, struct map *m) {
}

void apple_delete(struct apple *a) {
}
